name: Backport

on:
  pull_request:
    types:
      - closed
      - labeled

jobs:
  backport:
    runs-on: ubuntu-18.04
    name: Backport
    steps:
      - name: Backport
        uses: Gaurav0/backport@v1.0.24
        with:
          bot_username: c2c-backporter
          bot_token: f149a7a57b7fa4fbe0f0bae2bc506d8122811cdb
          bot_token_key: c663d9c66700043db65613aa60f11a117a64fc26
          github_token: ${{ secrets.GITHUB_TOKEN }}
