
templates:
  main: !template
    attributes:
      mapDef: !map
        width: 500
        height: 100
        dpi: [72, 96]
    processors:
    - !createMap
        inputMapper: {mapDef: map}
        outputMapper: {image: mapOut}
        imageType: TYPE_4BYTE_ABGR

