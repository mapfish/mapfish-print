
templates:
  main: !template
    processors:
      - !portMappingTestProcessor {}
      - !portMapping
        portMapping:
          8443: 9999
        hosts:
          - /.+\.com/
          - localhost
