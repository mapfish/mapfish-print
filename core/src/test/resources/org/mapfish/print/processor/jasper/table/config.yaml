throwErrorOnExtraParameters: true
templates:
  main: !template
    jasperTemplate: simpleReport.jrxml
    attributes:
      table: !table {}
      dependence: !integer {}
    processors:
    - !reportBuilder # compile all reports in current directory
      inputMapper: {dependence: dependence}
      directory: '.'
    - !prepareTable
      inputMapper: {table: table}
      outputMapper: {table: table}
