
templates:
  main: !template
    attributes:
      datasource: !datasource
        attributes:
          table: !table {}
          name: !string {}
          count: !integer {}
    processors:
    - !createDataSource
      processors:
        - !prepareTable {}

