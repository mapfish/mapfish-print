This example demonstrates the use of useGeodeticCalculations on the map when the projection is Pseudo-Mercator (e.g. EPSG:3857).

The map includes a layer with seven vertical lines, each 10m tall and spaced 10m apart.
You can verify that the generated graphic scale matches these lines.

